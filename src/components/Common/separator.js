import React from 'react';
import { View } from 'react-native';

const Separator = () => {
    return(
        <View
            style={{ height: 1, marginTop: 10, marginRight: 10, marginBottom: 10, backgroundColor: '#9E9E9E' }}
        />
    );
}

export default Separator;
